# 자동 최신화 웹사이트 배포 안내

이 저장소는 GitHub Pages에 올리면 평일 장 마감 후 자동으로 데이터를 갱신하고
웹사이트를 다시 배포하도록 준비되어 있습니다.

## 동작 방식

- 매주 월-금 17:10 KST에 자동 실행
- 코스피/코스닥 목록과 260거래일 차트 재수집
- `dist` 폴더 생성
- GitHub Pages에 자동 배포
- 수동 갱신도 GitHub Actions 화면에서 `Run workflow`로 가능

## GitHub에서 처음 한 번만 설정

1. GitHub 저장소의 `Settings`로 이동합니다.
2. `Pages` 메뉴를 엽니다.
3. `Build and deployment`의 `Source`를 `GitHub Actions`로 선택합니다.
4. `Actions` 탭에서 `Update and Deploy Dashboard`를 실행합니다.

실행이 끝나면 `Settings > Pages`에 접속 주소가 표시됩니다.

## 시간 변경

자동 실행 시간은 `.github/workflows/update-and-deploy.yml`의 cron 값으로 조정합니다.
GitHub Actions cron은 UTC 기준입니다.

예:

- 17:10 KST = `10 8 * * 1-5`
- 18:00 KST = `0 9 * * 1-5`
- 08:00 KST = `0 23 * * 0-4`
